# V2 live-harness tests

These tests exercise only disposable local fixtures and sanitized synthetic events. They do not invoke Codex, perform model inference, publish refs, or modify global Git configuration.
